runtime: java
env: flex

runtime_config:  # Optional
  jdk: openjdk11

handlers:
  - url: /.*
    script: this field is required, but ignored

manual_scaling:
  instances: 1

spring:
  jmx:
    enabled: false
